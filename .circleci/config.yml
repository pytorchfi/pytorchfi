version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  build-and-test:
    executor: python/default
    steps:
      - checkout
      - python/load-cache
      - python/install-deps
      - python/save-cache
      - run:
          command: python -m unittest -v
          name: Test
      - run:
          name: Code Coverage
          command: |
            $HOME/.local/bin/coverage run -m unittest -v
            $HOME/.local/bin/coverage report
            $HOME/.local/bin/coverage html
      - store_artifacts:
        path: htmlcov

workflows:
  main:
    jobs:
      - build-and-test
